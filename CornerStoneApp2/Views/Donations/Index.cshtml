@model IEnumerable<CornerStoneApp2.Models.Donation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.donorFirst)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.donorLast)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.donorZip)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.donorFirst)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.donorLast)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.donorZip)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DonationID })
        </td>
    </tr>
}

</table>
